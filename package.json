{
  "name": "pingwebsite",
  "version": "2.0.0",
  "description": "Website health monitor – checks if sites are reachable and sends SMS alerts",
  "module": "src/index.ts",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "build:docker": "sudo docker build -t pingweb ."
  },
  "dependencies": {},
  "devDependencies": {
    "bun-types": "^1.3.9"
  }
}
